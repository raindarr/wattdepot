<project name="qa" default="qa">
  <description>
    Compiles system, runs junit, checkstyle, findbugs, and pmd.  
  </description>

  <import file="build.xml" />
  <import file="checkstyle.build.xml" />
  <import file="findbugs.build.xml" />
  <import file="pmd.build.xml" />
  <import file="junit.build.xml" />

  <target name="qa" depends="compile, junit, checkstyle, pmd, findbugs" description="Verifies system.">
  </target>

    <!-- Would be even better if checkstyle, etc set a property on failure. Then we check that and
    open up the HTML output in a browser when it fails -->
  
</project>
